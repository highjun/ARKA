node_modules/
*.log
.output/
dist/
storybook-static/
.env
.env.*
.claude
docs/USER_NOTE.md

# 바깥 제품의 화면. 색인(`docs/references/description.json`)만 커밋하고 이미지는 각자
# `tools/references/capture.mjs`로 다시 만든다 — 공개 저장소에 남의 UI를 싣지 않는다.
docs/references/assets/

# 키·인증서 파일. 이름만 봐도 비밀인 것은 손이 닿기 전에 막는다.
*.pem
*.key
*.p12
*.pfx
*credential*

# CI 컨테이너용 가짜 값. `.env.*` 금지의 유일한 예외다 — 여기엔 시크릿이 없다.
!ops/deploy/.env.ci

# 복사해서 채우는 원본. 값이 없으므로 비밀이 아니다.
!ops/deploy/.env.example
