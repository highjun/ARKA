import type { Config } from "prettier";

/**
 * **코드의 모양은 여기가 정한다**. 포매터가 없던 동안 코드모드가 파일 하나를 납작하게
 *만든 채 머지된 일이 있었고(`Tab.tsx` 183줄), 리뷰도 린트도 그것을 못 봤다.
 *
 * 기본값을 그대로 쓴다 — 임의의 선택은 도구의 기본을 따르는 것이 관례다. **딱 하나만 어긋난다.**
 */
const config: Config = {
  /*
   * 기본값 80이 아니라 120이다. 이 저장소의 실측 분포가 p50=34·p90=103·**p95=120**·p99=139라
   * 80으로 접으면 3분의 1을 다시 쓰면서 JSX props가 세로로 길어진다. 120은 이미 쓰고 있는 폭이다.
   */
  printWidth: 120,
};

export default config;
