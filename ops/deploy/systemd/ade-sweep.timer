# 하루 한 번. **워크플로의 teardown을 대신하는 것이 아니라 그것이 놓친 것을 줍는다** —
# base 브랜치 삭제로 자동으로 닫힌 PR은 `closed` 이벤트를 쏘지 않는다(2026-09-11 실측).
[Unit]
Description=ADE 미리보기 청소 (하루 1회)

[Timer]
OnCalendar=daily
# 기계가 꺼져 있던 동안 놓친 실행을 켜자마자 한 번 돌린다.
Persistent=true
RandomizedDelaySec=30min

[Install]
WantedBy=timers.target
