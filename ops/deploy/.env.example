# 이 파일을 `.env`로 복사하고 빈칸을 채운다. compose가 **자기 옆의 `.env`**를 읽는다.
#
#     cp ops/deploy/.env.example ops/deploy/.env
#
# **실배포는 이 파일을 읽지 않는다** — 이 기계의 `~/ARKA/secure/env/arka.env`(0600)를 배포 잡이
# 셸에 풀고 compose를 부른다. 값이 리포에 사본으로 남지 않게 하는 것이 그 이유다.
#
# 여기 없는 `ARKA_HOST`·`ARKA_PORT`(컨테이너 안)·`ARKA_CLIENT_ROOT`·`ARKA_GIT_SHA`는
# **이미지가 굽는다.** [ ] 안은 등급이다. **[비밀]은 git에 넣지 않는다.**

# [설정] compose 프로젝트와 컨테이너 이름의 앞자리. 비우면 `arka` → `arka-app`·`arka-tunnel`.
ARKA_NAME=

# [설정] 띄울 이미지 태그. 비우면 `arka:local` — 없으면 그 자리에서 빌드한다.
ARKA_IMAGE=

# [설정] 컨테이너가 돌 uid/gid — `id -u`, `id -g`.
# 안 맞으면 컨테이너가 만든 파일을 호스트 사용자가 고치지 못한다.
ARKA_UID=
ARKA_GID=

# [설정] 워크스페이스로 마운트할 호스트 경로. **실재해야 한다** — 없으면 부팅에서 죽는다.
ARKA_WORKSPACE=

# [설정] `compose.local.yml`을 함께 쓸 때 호스트 루프백에 여는 포트. 비우면 3000.
ARKA_PORT=

# [설정] 배포 스모크가 두드릴 오리진 — `https://<호스트>`. **리포에 호스트 이름을 적지 않는다.**
ARKA_ORIGIN=

# [설정] cloudflared 설정·자격증명의 **호스트 경로** — `--profile tunnel`일 때만 쓰인다.
# 파일 자체는 저장소 밖(`~/ARKA/secure/cloudflared/`)에 산다. 호스트 이름과 터널 ID가 그 안에 있다.
ARKA_TUNNEL_CONFIG=
ARKA_TUNNEL_CREDENTIALS=
