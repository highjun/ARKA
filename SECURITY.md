# 보안

## 이 소프트웨어의 전제 — 먼저 읽을 것

**ARKA에는 인증이 없다.** 서버는 자신을 부르는 사람이 누구인지 묻지 않는다. 파일 읽기·쓰기와
git 실행이 워크스페이스 전체에 열려 있다.

이것은 실수가 아니라 결정이다. 바깥으로 나가는 길이
Cloudflare Tunnel 하나뿐이고 그 앞을 Cloudflare Access가 막는다는 전제 위에서만 성립한다.

**그래서 이 앱을 그냥 인터넷에 올리면 안 된다.** 포트를 열어 두는 순간 워크스페이스로 지정한
디렉터리 전체가 무인증으로 공개된다. 직접 세운다면 최소한 다음이 필요하다.

- 호스트 포트를 열지 않는다. 리버스 프록시나 터널이 유일한 경로여야 한다
- 그 앞에 인증 게이트를 둔다
- 워크스페이스를 **버려도 되는 디렉터리**로 잡는다

## 신고

취약점을 찾았다면 공개 이슈 대신 [Security Advisories](https://github.com/highjun/ARKASHIC/security/advisories/new)로
비공개 신고를 열어 주세요. 개인 프로젝트라 정해진 응답 시간은 없습니다.

## 범위 밖

- 위에 적은 "인증이 없다"는 전제 자체
- 로컬 접근이 있는 사람이 docker 네트워크로 컨테이너에 직접 닿는 것 — 같은 전제의 일부다
