{
  "name": "contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test:unit": "vitest run --exclude '**/*.integration.test.ts'"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "jiti": "^2.7.0",
    "ops": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
