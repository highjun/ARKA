{
  "name": "arka",
  "version": "0.0.0",
  "private": true,
  "description": "ARKA — 에이전트가 사용자의 워크스페이스를 다루는 개발 환경",
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "pnpm -r --if-present run lint && npx --prefix ops knip --config ops/knip.config.ts --no-config-hints",
    "test": "pnpm -r --if-present run test:unit && pnpm -r --if-present run test:integration",
    "build": "pnpm -r --if-present run build",
    "dev": "pnpm -r --parallel --if-present run dev",
    "prepare": "simple-git-hooks"
  },
  "devDependencies": {
    "simple-git-hooks": "^2.14.0"
  },
  "simple-git-hooks": {
    "pre-commit": "node ops/hooks/preCommit.ts",
    "pre-push": "node ops/hooks/prePush.ts"
  }
}
