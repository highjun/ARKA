---
id: TASK-46
title: view는 배치만 하고 세부는 component로 내린다 — 후보 B
status: To Do
created: 2026-09-10
updated: 2026-09-14
---

## 설명

후보 B. view가 UI를 직접 그리고 있어서 **스토리 없는 컴포넌트의 대역**을 하고 있다.

**실측**: view 12개 중 **9개**가 `@primer/react`를 직접 import한다. `git`·`search`에는
`component/` 폴더가 아예 없다.

view가 배치만 하면 view 스토리에서 볼 것이 "배치"로 좁혀져 컴포넌트 스토리와 겹치지 않는다
(TASK-45와 맞물린다).

**순수 변환 함수 9개는 `view/shared.ts`로 뺀다.** 그중 6개는 애초에 view를 떠날 수 없다 —
`ReactNode`를 반환하거나(Model·ViewModel은 `ReactNode`를 갖지 않는다) 레지스트리 조회가
필요하다. 갈 수 있는 3개만 ViewModel로 보내면 변환이 두 군데로 갈라져 오히려 나빠진다.

**작지 않다** — view 스토리 35개 중 상당수가 바뀌고 `git`·`search`에 `component/`를 새로 만든다.

## 인수 조건

- [ ] `view/`에서 `@primer/react` 직접 import가 **0건**이 된 뒤 `no-restricted-imports`로 켠다
- [ ] `git`·`search`에 `component/`가 생기고 각 컴포넌트에 스토리가 있다
- [ ] 순수 변환 함수가 `view/shared.ts`에 모인다

## 2026-09-14 — 채택, 범위를 좁혀서

후보 B를 **채택했다.** 범위는 좁혔다 — **view 안에 있는 컴포넌트만 내린다.** view 자체의
스토리 범위(후보 A)와 값 파일(후보 C)은 여기 딸려 가지 않는다.

첫 벌은 **TASK-60**이 한다 — `ChangeList`·`SearchResultList`·`DiffView`·`KeybindingTable` 넷을
내리고 `git`·`search`에 `component/`를 만든다. 근거는 [ADR 0007](../adr/0007-client-layers.md)
(`component/`는 props만 받는 자리다).

TASK-60이 끝나고도 **이 태스크에 남는 것 둘**이다.

- `view/`에서 `@primer/react` 직접 import를 0건으로 만든 뒤 `no-restricted-imports`로 켜기. 넷을 내려도 나머지 view가 Primer를 직접 쓴다 — 배치용 `Stack`·`Box`까지 막을지가 판단거리다.
- 순수 변환 함수 아홉을 `view/shared.ts`로 모으기(이름은 TASK-49와 함께 정한다).
