---
id: TASK-79
title: ADR 0015 — 룰셋을 코드 오너 승인으로 좁히고 CODEOWNERS를 정독 자리에 맞춘다
status: To Do
labels: [review, user]
created: 2026-09-20
---

## 설명

사용자만 할 수 있는 일이다(GitHub 설정). TASK-78 뒤에 한다 — 리뷰 에이전트가 없는 채로 풀면 층 2가 비어 있다(→ [ADR 0015](../adr/0015-review-tiers.md)).
룰셋 `main`에서 "승인 1개"를 "코드 오너 리뷰 필수"로 바꾸고 자동 머지를 켠다. `.github/CODEOWNERS`에 규약 검토 강도표의 정독 열을 옮긴다 — `packages/contracts/`, `model/`·`viewmodel/`의 `I*.ts`, 각 슬라이스의 `index.ts`, 린트·tsconfig 설정. 알림은 needs-human 라벨 하나로 좁힌다.

## 인수 조건

- [ ] CODEOWNERS만 건드린 PR은 사람 승인 없이 머지되지 않고, 그 밖만 건드린 PR은 초록이면 머지된다
- [ ] CODEOWNERS 항목이 규약 검토 강도표의 정독 열과 같다
- [ ] ADR 0003·규약 "PR과 머지"가 새 절차를 가리킨다
