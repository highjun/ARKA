---
id: TASK-72
title: 설정의 enum 옵션에 사람이 읽는 라벨을 둔다
status: To Do
created: 2026-09-18
---

## 설명

`SettingsDescriptor`(`type: "enum"`)의 `options`는 값 문자열뿐이라 설정 탭이 `auto`·`compact`·`touch`를 그대로
보인다. 옛 화면은 "촘촘하게 (IDE 밀도)"처럼 적었다. 계약 검토가 먼저다(→ `docs/contracts/core.md` 설정 절).

## 인수 조건

- [ ] `options`가 `{ value, label }`을 받거나 `labels`가 따로 있다 — 하나로 정한다
- [ ] `SettingsTabView`가 라벨을 그린다. `registerServices.test.tsx`의 `getByLabelText("touch")`를 라벨로 되돌린다
