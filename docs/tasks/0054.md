---
id: TASK-54
title: C2 컴포넌트 규약을 기계적으로 적용한다 — 평탄화·삭제·ref prop
status: Done
created: 2026-09-14
updated: 2026-09-14
---

## 설명

[ADR 0008](../adr/0008-component-surface.md)이 정한 것 중 **판단이 필요 없는 것만** 한 라운드로 모은다.
컴포넌트마다 흩어 놓으면 같은 치환을 여덟 번 검토하게 된다.

- `shared/components/{common,layout,markdown}/`을 **`shared/component/`로 평평하게** 옮기고 별칭을 `#component/<Name>`로 둔다. 그룹 배럴 셋과 루트 배럴을 지우고 경로 import로 바꾼다 — 배럴이 없으면 "배럴에서 빠뜨렸다"가 생기지 않는다(넷이 빠져 있었다).
- **아무도 안 쓰는 것 열을 지운다** — `Bold`·`Italic`·`Highlight`·`Code`(원소 하나에 선언 한 줄), `Collapsible`, `Divider`, `Slider`, `ShortcutBar`, `List`, `QuoteBlock`. `@radix-ui/react-slider` 의존성도 함께 나간다.
- **`forwardRef`를 `ref` prop으로 바꾼다**(34파일). 하네스의 `implementsForwardRef`는 보는 것이 그대로이므로 이름만 `implementsRef`로 고친다.
- **자작 셋을 기성품으로 바꾼다** — `useControlledState` → `@radix-ui/react-use-controllable-state`(지금은 전이 의존성이라 직접 의존성으로 올린다), `mergeClassNames` → `clsx`(12곳), `assembleCompound` → `Object.assign`(부품 없는 17곳은 그냥 export).
- **`<Name>RootProps` + 별칭을 `<Name>Props` 하나로** 줄이고, `style`을 `Omit`으로 막던 여덟 곳을 통과시킨다.

## 인수 조건

- [x] `shared/component/` 아래가 평평하고 `shared/components/`가 없다
- [x] `git grep -l 'shared/components'`가 비어 있다
- [x] `git grep -c forwardRef -- 'packages/client/src'`가 0이다
- [x] `useControlledState`·`mergeClassNames`·`assembleCompound`·`<Name>RootProps`가 0건이다
- [x] `@radix-ui/react-use-controllable-state`가 `packages/client/package.json`의 직접 의존성이다
- [x] `pnpm --filter ops check`가 초록이고 스토리북에서 그림이 이전과 같다

## 2026-09-14 — 둘로 갈랐다

여섯 관심사가 한 PR에 들어가면 검토가 서지 않는다(큰 덩어리로 갔다가 전체를 롤백한 전례가 있다).

- **첫 벌 (PR #37, 끝)** — 파일이 움직이고 사라지는 것. 평탄화·별칭·배럴 다섯 삭제·컴포넌트 열 삭제·`@radix-ui/react-slider` 제거. 위 인수 조건 첫 둘이 여기서 닫혔다.
- **둘째 벌 (PR #38, 끝)** — 이름과 유틸 치환. Radix `useControllableState`, `clsx`, `Object.assign`, `<Name>Props` 이름, `style` 통과. 자작 유틸 셋이 지워졌다.
- **셋째 벌 (PR #39, 끝)** — `ref` prop 전환. 25곳을 풀고 하네스 이름을 `implementsRef`로 바꿨다.

첫 벌에서 곁가지로 드러난 것: 없어진 폴더를 가리키는 주석 아홉이 **이동 전부터 이미 틀렸다.**
`shared/components`에 있다던 `Tab`·`ThemeProvider`·`FileTreeItem`이 각각 `workbench/component/Tab`·
Primer·`FileTree`에 있었다. 제 자리를 가리키게 고쳤다.

## 2026-09-14 — 남긴 것

ADR 0008이 정한 것 중 **props 바탕**(`HTMLAttributes` → `ComponentPropsWithoutRef`)만 남았다.
관심사가 다르고 원소 클래스에서 태그 이름으로 가는 대응이 기계적이지 않아 **TASK-63**으로 뺐다.

`ref` 전환에서 덤으로 드러난 것: `forwardRef(...)` 호출은 린트의 JSDoc 규칙이 함수로 보지 않아서,
화살표 함수로 풀리자 **공개 컴포넌트 일곱에 문서가 없다는 것이 드러났다.** 채웠다. 규칙이 이미
있었는데 문법 모양 하나에 가려져 있던 자리다.
