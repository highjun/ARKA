---
id: TASK-69
title: 탭·설정·단축키 재정의를 서버에 저장한다
status: To Do
created: 2026-09-18
---

## 설명

`ITabLayout`·`ISettings`·`ICommandService.overrides`가 전부 localStorage에 산다(`workbench.tabTree`·
`workbench.settings`·`workbench.keybindings`). 계약은 "서버가 저장한다 — 기기가 바뀌어도 같다"이다
(→ `docs/contracts/workbench.md` 탭 절). 커널은 저장소 포트(`IStorage`·`SettingsStore`·`overridesStore`)만
알아 바꿀 자리는 조립부(`workbenchModule.tsx`)뿐이다.

## 인수 조건

- [ ] 서버에 `settings.json`(설정·재정의)과 탭 트리를 읽고 쓰는 라우트가 있다
- [ ] 포트 셋의 실물이 HTTP다. localStorage 실물은 지운다
- [ ] 첫 부팅에 옛 localStorage 값을 한 번 올린다(이관) — 그 뒤 로컬은 안 본다
