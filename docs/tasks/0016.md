---
id: TASK-16
title: 테스트 관련 전역 폴더 정리 — client/e2e·test/vrt·test/contract
status: Done
priority: low
labels: [tooling, docs]
created: 2026-09-08
---

## 설명

USER_NOTE: client/test/{vrt,e2e}로 묶을지. 지금은 e2e는 client/e2e, VRT·contract는 루트 test/. 한 규칙으로 정한다.

## 결과

2026-09-09~10에 걸쳐 해소. `test/contract`는 서버의 응답 계약으로 갈려 사라졌고(`9531019`),
`e2e`와 `vrt`는 `packages/client/test/` 아래로 묶였다(`d299f0c`). 루트 `test/`는 없어졌다.

한 규칙으로 정한 것은 **"테스트가 대상을 특정할 수 있는가"**다 — 특정할 수 있으면 대상 옆,
없으면 그 패키지의 `test/`. 앱 전체를 보는 E2E, 스토리 전부를 도는 VRT, 테스트 환경을 세우는
`vitestSetup.ts`가 뒤쪽이다.
