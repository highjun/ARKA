---
id: TASK-74
title: URI.basename — 경로의 마지막 조각을 세 곳이 각자 자른다
status: To Do
created: 2026-09-18
---

## 설명

`path.split("/").pop() ?? path`가 `TabSystemViewModel`·`textTabProvider`·`previewTabProvider`에 따로 있다.
`contracts`의 `URI`에 `basename`(또는 `name`)을 두면 한 곳이 된다. `URI`는 `contracts` 패키지라 서버와 함께 본다.

## 인수 조건

- [ ] `URI.basename` 추가 — 테스트
- [ ] 세 곳이 그것을 쓴다
