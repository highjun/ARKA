---
id: TASK-17
title: index.html을 src/workbench로 옮길지
status: Done
priority: low
labels: [tooling]
created: 2026-09-08
---

## 설명

vite root 설정이 따라온다. 결정만 필요.

## 결과

2026-09-10 옮겼다. 근거는 성격이다 — 셸을 띄우는 진입점이라 `workbench/`의 일이다.

`root`가 따라오면서 세 가지가 함께 움직였다. `publicDir`은 패키지 루트로 되돌리고,
`outDir`은 절대경로로 바꾸고, **`test.root`를 패키지 루트로 고정**했다. 마지막 것이
없으면 vitest가 `src/workbench` 안만 뒤져 112개 중 28개만 돈다 — 한 설정 파일을
vite·vitest·Storybook 셋이 함께 읽기 때문이다.

`public/` 아래로 두는 안은 실험에서 빌드가 죽었다. `public/`은 가공 없이 복사되는
자리라 진입점이 될 수 없다.
