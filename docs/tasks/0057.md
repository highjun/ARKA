---
id: TASK-57
title: C5 마크다운을 React 렌더러로 그린다 — 채팅에도
status: Done
created: 2026-09-14
updated: 2026-09-14
---

## 설명

마크다운을 **채팅에서 안 그린다.** 에이전트 답이 `text`로 와서 그대로 찍힌다. 지금 렌더링은 파일
미리보기 탭 하나뿐이고 방식이 HTML 문자열(marked+DOMPurify)이다.

많이 쓸 곳이 채팅이고 거기서 필요한 것이 **상호작용**이다 — 코드 블록 복사, 파일 링크로 워크스페이스
열기, 체크박스. HTML 문자열에는 그것을 붙일 자리가 없다. react-markdown은 요소마다 컴포넌트를 꽂는
표준이라 이미 복사 버튼을 가진 `CodeBlock`이 그대로 `code` 요소가 된다.

- `shared/component/Markdown` 신규 — react-markdown + remark-gfm. props는 `source`와 `components?`(요소 덮어쓰기 — view가 파일 링크 열기를 꽂는 자리).
- `code`만 `CodeBlock`으로 바꾸고 나머지 원소는 `Markdown.module.css`가 입힌다. **raw HTML을 켜지 않는다** — 그래서 정화기가 필요 없다.
- `Message` 본문과 `StepBlock` 도구 출력, 미리보기 탭이 모두 이것을 쓴다. `MarkdownPreview`와 `renderMarkdown.ts`, `marked`, `dompurify`가 나간다.
- 스트리밍 중 메시지마다 다시 파싱한다 — 메시지 단위 memo로 막는다.

## 인수 조건

- [ ] `marked`·`dompurify`가 의존성에 없고 `renderMarkdown.ts`가 없다
- [ ] 채팅 메시지에서 코드 블록이 그려지고 복사가 된다
- [ ] 미리보기 탭과 채팅이 같은 마크다운에 같은 그림을 낸다
- [ ] `<script>`가 든 마크다운이 스크립트로 실행되지 않는다(테스트)
- [ ] 메시지 목록이 긴 상태에서 스트리밍 중 재렌더가 메시지 하나로 묶인다

## 2026-09-14 — 바꿨다

`shared/component/Markdown` 신규. react-markdown 10 + remark-gfm. `MarkdownPreview`와
`renderMarkdown.ts`, `marked`, `dompurify`가 나갔다.

**루트는 `div`다.** 처음에 `article`로 두었는데 채팅 말풍선(`Message`가 `article`이다) 안에 들어가면
중첩된다. 미리보기 탭 쪽은 이미 자기 `article`을 두르고 있어 잃는 것이 없다.

**코드 블록만 컴포넌트로 바꾼다.** 나머지 원소는 `Markdown.module.css`가 입힌다 — GitHub의
`markdown-body` 방식이다. 언어를 적은 펜스만 `CodeBlock`이 되고 인라인과 언어 없는 펜스는
`code` 원소로 남는다(react-markdown 10이 인라인에 `className`을 주지 않아 그것으로만 가른다).

**적용한 자리 셋** — 미리보기 탭, 채팅의 사용자·에이전트 본문, `StepBlock`의 생각 요약.
생각 요약은 모델이 쓴 산문이라 마크다운이 맞고, 그래서 `summary`의 타입을 `ReactNode`에서
`string`으로 좁혔다.

**도구 입력·출력은 그대로 `<pre>`다.** JSON이나 원시 출력이라 마크다운이 아니고, `CodeBlock`으로
바꾸면 접힌 단계 안에 캡션과 복사 버튼이 둘씩 생겨 무거워진다. 별 라운드로 볼 일이다.

원문 HTML은 켜지 않았다 — 그래서 정화기가 필요 없고 `<script>`가 글자로 남는다(테스트로 확인).
